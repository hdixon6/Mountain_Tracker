services:
  - type: web
    name: mountain-tracker
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mountain-tracker-db
          property: connectionString

databases:
  - name: mountain-tracker-db
    plan: free